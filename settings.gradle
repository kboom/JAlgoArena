rootProject.name = 'jalgoarena-e2e'

['JAlgoarena-API', 'JAlgoArena-Auth', 'JAlgoArena-Eureka', 'JAlgoArena-Judge', 'JAlgoArena-Problems', 'JAlgoArena-Submissions', 'JAlgoArena-UI'].each {
    include ":$it"
//    project(":$it").rootDir = new File("./$it")
}